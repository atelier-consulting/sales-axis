header.border-b.border-b-concrete
  .box.h-16.flex.justify-between.items-stretch
    h1 SalesAxis
    nav.auth.flex.items-center
      a.block.ml-4.border.text-xs.text-monsoon.border-concrete.px-4.py-2.rounded-sm(class="md:text-sm md:py-1") Log in
      a.block.ml-4.bg-brand-blue.text-white.text-xs.px-4.py-2.rounded-sm(class="md:text-sm md:py-1") Sign up

section.pt-6.pb-4
  +trending_product(
    "Business Network for Logistics",
    "It is a long established fact that a reader will be distracted by the readable content of a page when looking at its layout.",
    "12%",
    "500K USD",
    "trending-product@1x.png",
  )

section.top-products
  .box
    h2 Top Products by Leads Industries 
  +swipe(3)
    +card(
      "Southeast Asia market research and management consulting services",
      "Consulting",
      "product-image-1@2x.jpg",
    )
      +attributes("vertical md:horizontal")
        +attr("Commission", "15%", "highlight", "%",)
        +attr("Project size range", "$60K+", "accent", "$")

    +card(
      "Corporate Management Software & Enterprise Resource Planning",
      "Management",
      "product-image-2@2x.jpg",
    )
      +attributes("horizontal md:vertical")
        +attr("Commission", "20%", "highlight", "%")
        +attr("Project size range", "$80K+", "accent", "$")

    +card(
      "Wholesale Consumer Goods & Personal Protection Equipment",
      "Healtcare",
      "product-image-3@2x.jpg",
    )
      +attributes("horizontal md:horizontal")
        +attr("Commission", "4%", "highlight", "%")
        +attr("Project size range", "$100K+", "accent", "$")

section.top-lead-types.pb-4
  .box
    h2 Top Successful Type of Leads
  +swipe(3, "md:vertical")
    +panel(
      "Manufacturing execution system",
      "Operation Director / COO",
      "Do you have any leads match the same position? Let's connect and earn commission.",
      "product-image-2@2x.jpg",
    )
      +attributes("vertical md:vertical")
        +attr("Conversion rate", "27%", "highlight")
        +attr("Average commission", "$10K+", "accent")

    +panel(
      "Fieldglass Contingent Workforce Management",
      "HR Director / COO",
      "Do you have any leads match the same position? Let's connect and earn commission.",
      "product-image-3@2x.jpg",
    )
      +attributes("vertical md:vertical")
        +attr("Conversion rate", "19%", "highlight")
        +attr("Average commission", "$14K+", "accent")

    +panel(
      "Enterprise resource planning",
      "Business Owner/ CEO / COO",
      "Do you have any leads match the same position? Let's connect and earn commission.",
      "product-image-1@2x.jpg",
    )
      +attributes("vertical md:vertical")
        +attr("Conversion rate", "41%", "highlight")
        +attr("Average commission", "$50K+", "accent")

section.about.pt-10.pb-24.bg-concrete
  .box
    h2 Who We Are
    p.max-w-2xl.mx-auto.px-4.leading-6.text-center.text-sm.text-monsoon(class="md:leading-7 md:text-lg md:font-light")
      | Sales Axis is the platform for solution providers who are looking
      | to connect with more front end sales & consultants. And it's for
      | front end sales who are looking for new solution to sell. Simply
      | fill out the required information, and you'll get matched with
      | relevant solution providers.

footer.text-white.bg-pearl
  .box.flex.flex-col.py-8(class="md:flex-row md:pt-14")
    section.app-description.flex-1.pr-16
      h1.small Sales Axis
      p.text-xs.pt-3.max-w-prose(class="md:text-sm md:pt-4")
        | Our webapp, Sales Axis, is the platform for solution providers who
        | are looking to connect with more front end sales & consultants.

    section.links.flex-1.flex.flex-row.py-8(class="md:py-0 md:pr-8")
      article.flex-1.pr-8
        h3.font-medium.leading-snug(class="md:text-lg md:leading-snug") Pages
        ul.pt-2.text-sm.text-monsoon.leading-8
          li: a(href="#") Product View
          li: a(href="#") User dashboard
          li: a(href="#") User's Lead page
      article.flex-1
        h3.font-medium.leading-snug(class="md:text-lg md:leading-snug") About
        ul.pt-2.text-sm.text-monsoon.leading-8
          li: a(href="#") Our Story
          li: a(href="#") Privacy policy

    section.newsletter.flex-initial
      h3.font-medium.leading-snug(class="md:text-lg max-w-[12rem] md:leading-snug") Subscribe to our newsletter
      form.flex.py-4(class="md:pt-8")
        input.block.h-12.flex-1.bg-transparent.border-b.text-sm(class="border-b-white/10", type="email", placeholder="Email address")
        input.block.h-12.w-12.bg-brand-blue.text-white.rounded-t-lg(type="submit", value=">")
      ul.social.flex.justify-end(class="md:justify-start")
        +social_icon("f", "#")
        +social_icon("i", "#")
        +social_icon("t", "#")
        +social_icon("l", "#")

footer(class="bg-pearl border-t border-t-[#103658] text-tuna text-xxs md:text-xs text-center")
  .box.py-8 &copy; SALES AXIS. All rights reserved

script.
  const swipes = document.querySelectorAll(".swipe")
  swipes.forEach(swipe => {
    const obs = new IntersectionObserver(
      entries => { entries.forEach(e => e.target.classList.toggle("shadow-lg", e.isIntersecting)) },
      { root: swipe, threshold: .75, }
    )
    const items = swipe.querySelectorAll(".track>*")
    items.forEach(item => { obs.observe(item) })
  })
